import { FileType } from "../interfaces/Folder.types";

export const DEFAULT_FOLDER_FILE_TYPE = FileType.ANY;

export const DEFAULT_FOLDER_QUOTA = Infinity;

export const DEFAULT_FOLDER_MAX_FILE_SIZE = Infinity;
